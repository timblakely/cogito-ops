---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: litellm
spec:
  interval: 1h
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # litellm-helm 1.97.0. Unlike LLMKube, LiteLLM publishes its chart as OCI
    # from the main repo, so the digest is pinnable the same way llm-operator's
    # is. This is an immutable promotion record.
    digest: sha256:83d2427c380bdc7f8af1560614c570b03a28ef836dd5cada0ed595f175a8b9d9
  url: oci://ghcr.io/berriai/litellm-helm
