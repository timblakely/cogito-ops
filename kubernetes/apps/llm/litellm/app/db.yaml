---
# Attaches to the litellm-db Cluster the cnpg/init component generates.
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: litellm
spec:
  name: app
  owner: app
  cluster:
    name: litellm-db
