---
# Non-production llama.cpp validation: the operator switches from the stable
# vLLM Gemma path to the registered, cache-hot Laguna backend.
apiVersion: llm.cogito.dev/v1alpha1
kind: LLMActiveModel
metadata:
  name: default
  namespace: llm
spec:
  modelName: poolside/Laguna-S-2.1
