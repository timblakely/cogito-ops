---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: llm-operator
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # Chart 0.1.26 adds the normalized llm_* proxy metrics (request/TTFT/token
    # instrumentation, dual-backend KV cache gauge). This is an immutable
    # promotion record; its resources are adopted in-place below.
    digest: sha256:51219b9af32bf993f78048c52ba480958518f7c56dc8861598279bdb74b31688
  url: oci://ghcr.io/timblakely/charts/llm-operator
