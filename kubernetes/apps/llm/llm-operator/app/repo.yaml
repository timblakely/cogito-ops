---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: llm-operator
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # Chart 0.1.28 adds the (still admission.enabled=false by default) cert-
    # manager Issuer/Certificate, webhook Service, and
    # ValidatingWebhookConfiguration templates needed to actually turn on
    # admission.enabled - nothing in this bump changes behavior on its own.
    # This is an immutable promotion record; its resources are adopted
    # in-place below.
    digest: sha256:21fcf2b2e48a8141cd5d000f4a60d165f64f67f1b1036ea5a191254777165d2b
  url: oci://ghcr.io/timblakely/charts/llm-operator
