---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: llm-operator
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # Chart 0.1.25 authorizes the explicitly configured dedicated proxy
    # backends. This is an
    # immutable promotion record; its resources are adopted in-place below.
    digest: sha256:485b616ce7e27ad2c643b6e8c53dac844a882f720d3abc43cb611b8cf781bdec
  url: oci://ghcr.io/timblakely/charts/llm-operator
