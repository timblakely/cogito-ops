---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: llm-operator
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # Chart 0.1.29 drops the now-dead ENABLE_DEPLOYMENT_MUTATIONS env var from
    # the proxy Deployment template, matching timblakely/llm-operator@eef24a8a0189
    # retiring the legacy direct-mutation code path entirely. This is an
    # immutable promotion record; its resources are adopted in-place below.
    digest: sha256:71569cffc4921c65d045bda7a3b993b51fd770d41c05267d784613c982de8ddb
  url: oci://ghcr.io/timblakely/charts/llm-operator
