---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: llm-operator
spec:
  interval: 5m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    # Chart 0.1.27 fixes the ActiveModel Transitioning/Failed flap and the
    # proxy's dead Failed-transition detection (waitForOperatorTransition
    # compared against status.modelName, which never matches during a
    # failure). This is an immutable promotion record; its resources are
    # adopted in-place below.
    digest: sha256:f5ce0f756769b545d28363b19b578cd1bef63582ab6da32cb5083bf0f6cdbbc2
  url: oci://ghcr.io/timblakely/charts/llm-operator
