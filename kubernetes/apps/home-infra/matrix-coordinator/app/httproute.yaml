---
# GitHub needs one public, signature-verified callback. No coordinator UI or
# internal API is exposed by this route.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: matrix-coordinator-github-webhook
spec:
  hostnames:
    - cogito-webhook.${DOMAIN_NAME}
  parentRefs:
    - name: envoy-external
      namespace: envoy-system
  rules:
    - matches:
        - path:
            type: Exact
            value: /events/github
          method: POST
      backendRefs:
        - name: matrix-coordinator
          port: 8080
