---
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: matrix
spec:
  clientID: synapse
  launchUrl: https://${APP_URL}
  callbackUrls:
    - https://${APP_URL}/_synapse/client/oidc/callback
  allowedUserGroups:
    - name: matrix
      namespace: ${NS}
