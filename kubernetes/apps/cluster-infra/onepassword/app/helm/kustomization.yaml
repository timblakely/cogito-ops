---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: onepassword
namespace: ${NS}
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: onepassword-helm-values
    files:
      - ./values.yaml
