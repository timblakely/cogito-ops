[project]
name = "cogito-matrix-coordinator"
version = "0.1.0"
description = "Matrix planning and Foreman workload gateway"
requires-python = ">=3.12"

[project.scripts]
cogito-coordinator = "coordinator.service:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
