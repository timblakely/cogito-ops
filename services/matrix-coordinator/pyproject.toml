[project]
name = "cogito-matrix-coordinator"
version = "0.1.0"
description = "Harness-neutral Matrix development coordinator"
requires-python = ">=3.12"

[project.scripts]
cogito-coordinator = "coordinator.service:main"
cogito-adapter = "coordinator.adapter:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
