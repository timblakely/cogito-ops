# vLLM patches

Add one version-pinned patch per upstream compatibility gap. Each patch must name
the upstream issue or pull request, the base image version it applies to, and a
removal condition for when an upstream release contains the equivalent fix.
